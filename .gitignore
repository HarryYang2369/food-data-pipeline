# 环境变量（含 API key，绝对不上传）
.env
.env.local

# 浏览器登录 session（含小红书 cookie，绝对不上传）
xhs_session/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# macOS
.DS_Store

# 爬取的数据（量大，不放 git）
data/*.json

# Playwright 浏览器（自动下载，不需要 commit）
playwright-browsers/
