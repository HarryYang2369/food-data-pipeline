# 复制这个文件为 .env，填入你的 key。
# 修改后不需要重启，直接运行 python run.py 就会读到。

# ─── Claude API（可选）────────────────────────────────────────────────────────
# 用来把爬到的帖子提取成结构化餐厅数据（名称 / 地址 / 菜系 / 亮点）。
# 没有 key 也能跑，只是会跳过提取步骤，先把原始帖子存下来。
# 申请地址：https://console.anthropic.com → API Keys → Create Key
ANTHROPIC_API_KEY=

# ─── 将来连 Firestore（可选，目前不需要）─────────────────────────────────────
# 如果想把爬到的数据直接写进 WTF app 的 Firestore，到时候再加这个。
# GOOGLE_APPLICATION_CREDENTIALS=path/to/serviceAccountKey.json
